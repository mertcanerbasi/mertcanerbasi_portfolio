import 'package:flutter/material.dart';

class Constants {
  static const primaryColor = Color(0xFF000000);
  static const accentColor = Color(0xFFC19957);
  static const textColor = Color(0xFFDBDBDB);

  static const defaultPadding = 16.0;
}
