name: mertcanerbasi
description: "Mertcan Erbaşı's personal website"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.4.0

dependencies:
  cupertino_icons: ^1.0.8
  firebase_core: ^3.3.0
  flutter:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_svg: ^2.0.10
  google_fonts: ^6.2.1
  provider: ^6.1.2
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
